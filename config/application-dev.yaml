app:
  name: "ai-genai-finetuning"
training:
  base_model: "meta-llama/Llama-3.1-8B-Instruct"
  num_epochs: 1
  batch_size: 2
  max_seq_length: 512
  save_steps: 50
lora:
  rank: 8
  alpha: 16
api:
  host: "127.0.0.1"
  port: 8000
  reload: true
logging:
  level: "DEBUG"
  format: "text"
  file: "logs/app-dev.log"
